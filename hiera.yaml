---
:backends:
  - yaml
:hierarchy:
  - "%{clientcert}"
  - "%{role}"
  - "%{virtual}/%{operatingsystem}"
  - "%{operatingsystem}"
  - global

:yaml:
  :datadir: "/etc/puppet/hieradata/%{environment}"

